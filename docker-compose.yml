services:
  app:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    entrypoint: /app/entrypoint.sh
    stdin_open: true
    tty: true

volumes:
  node_modules:
